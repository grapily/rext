TO DO
_ add options parameter presence check in update and relative test
_ add options parameter presence check in retrieve and relative test
_ add options parameter presence check in destroy and relative test
_ testing version is a well-formed string /\d\d?\.\d\d?\.\d\d?/
_ testing document name is fs compliant (e.g. not contain '*/...')
_ after destroyin latest version of a document, latest directory needs to point to new latest one
_ what happened if i destroy the only version of a document?